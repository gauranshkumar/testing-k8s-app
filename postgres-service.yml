apiVersion: v1
kind: Service
metadata: 
  labels: 
    app: testing-k8s-vote-app
    name: db-service
  name: db
spec: 
  ports: 
    - 
      port: 5432
      targetPort: 5432
  selector: 
    app: testing-k8s-vote-app
    name: postgres-pod
  type: ClusterIP

