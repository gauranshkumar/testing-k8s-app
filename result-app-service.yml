apiVersion: v1
kind: Service
metadata:
        name: result-app-service
        labels:
                name: result-app-service
                app: testing-k8s-vote-app

spec:
        type: LoadBalancer
        ports:
                - targetPort: 80
                  port: 80
        selector:
                name: reult-app-pod
                app: testing-k8s-vote-app


                  
