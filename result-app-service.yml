apiVersion: v1
type: Service
metadata:
        name: result-app-service
        lables:
                name: result-app-service
                app: testing-k8s-vote-app

spec:
        type: NodePort
        ports:
                - targetPort: 80
                  port: 80
        selector:
                name: reult-app-pod
                app: testing-k8s-vote-app


                  
