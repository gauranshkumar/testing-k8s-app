apiVersion: v1
type: Pod
metadata:
        name: redis-pod
        lables:
                name: redis-pod
                app: testing-k8s-vote-app

spec:
        containers:
                - name: redis-container
                  image: redis
                  ports:
                          - containerPort: 6379
                
