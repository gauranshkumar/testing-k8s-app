apiVersion: v1
type: Pod
metadata:
        name: postgres-pod
        lables:
                name: postgres-pod
                app: testing-k8s-vote-app

spec:
        containers:
                - name: postgres-container
                  image: postgres:9.4
                  ports:
                          - containerPort: 5432
                
