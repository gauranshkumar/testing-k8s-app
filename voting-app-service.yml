apiVersion: v1
type: Service
metadata:
        name: voting-app-service
        lables:
                app: testing-k8s-vote-app

spec:
        type: LoadBalancer
        ports:
                - targetPort: 80
                  port: 80
        selector:
                name: voting-app-pod
                app: testing-k8s-vote-app

                  
